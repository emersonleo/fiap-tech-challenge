version: '3.8'

services:
  horadoremedio:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
    depends_on:
      - mysql
    volumes:
      - horadoremedio-logs:/var/log/horadoremedio

  mysql:
    image: mysql:9.4.0
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: horadoremedio
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mysql-database:/var/lib/mysql

volumes:
  mysql-database:
    driver: local
  horadoremedio-logs:
    driver: local
